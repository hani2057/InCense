import React from "react";

const ProfileInfo = () => {
  return (
    <div style={{ width: "12.5rem", position: "fixed", left: "0" }}>
      ProfileInfo
    </div>
  );
};

export default ProfileInfo;
