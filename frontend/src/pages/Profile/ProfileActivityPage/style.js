import styled from "styled-components";
import { FlexDiv } from "../../../components/common/FlexDiv/FlexDiv";

const ProfileActivityItemContainer = styled(FlexDiv)`
  height: auto;
  flex-wrap: wrap;
  justify-content: start;
  margin-bottom: 12rem;
`;

export { ProfileActivityItemContainer };
