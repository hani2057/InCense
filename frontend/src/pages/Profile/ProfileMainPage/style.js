import styled from "styled-components";

const ProfileOutletContainer = styled.div`
  width: 100%;
  padding-left: 12.5rem;
`;

const ProfileMainBtnWrapper = styled.div`
  width: calc(100% / 3);
`;

export { ProfileOutletContainer, ProfileMainBtnWrapper };
